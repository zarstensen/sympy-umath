[project]
name = "sympy-umath"
version = "0.2.0"
description = ""
authors = [
    {name = "Zarstensen"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "sympy",
    "uncertainties"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
